using UnityEngine;
using System.Collections;

namespace KofTools{
	public class PressurePlate : TriggerObject {
		
		bool triggerOnWater;
		float waterTriggerDepth;
		
		// Use this for initialization
		new void Start () {
			base.Start();
		}
		
		// Update is called once per frame
		void Update () {
		
		}
	}
}
